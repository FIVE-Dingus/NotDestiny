#include "Window.h"

/*WindowObject::WindowObject(SDL_Texture* tex, int x, int y, int w, int h) : texture(tex) {
    rect.x = x;
    rect.y = y;
    rect.w = w;
    rect.h = h;
}

void WindowObject::render(SDL_Renderer* renderer) {
    SDL_RenderCopy(renderer, texture, nullptr, &rect);


}*/
